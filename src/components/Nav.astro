---
// Minimal nav — just scroll links
---

<nav class="nav" id="nav">
  <div class="nav__inner container">
    <a href="#top" class="nav__logo" aria-label="Too Bad Tattoo Studio - back to top">
      <span class="nav__logo-mark">
        <span class="nav__logo-top">Too Bad</span>
        <span class="nav__logo-bottom">Tattoo</span>
      </span>
    </a>

    <button class="nav__toggle" aria-label="Toggle navigation" aria-expanded="false">
      <span class="nav__toggle-line"></span>
      <span class="nav__toggle-line"></span>
    </button>

    <ul class="nav__links" role="list">
      <li><a href="#about">About</a></li>
      <li><a href="#artists">Artists</a></li>
      <li><a href="#visit">Visit</a></li>
      <li>
        <a href="https://www.instagram.com/toobadtattoostudio/" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <rect x="2" y="2" width="20" height="20" rx="5"/>
            <circle cx="12" cy="12" r="5"/>
            <circle cx="17.5" cy="6.5" r="1.5" fill="currentColor" stroke="none"/>
          </svg>
        </a>
      </li>
    </ul>
  </div>
</nav>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 1rem 0;
    background-color: transparent;
    transition: background-color 0.3s ease, box-shadow 0.3s ease;
  }

  .nav.scrolled {
    background-color: rgba(245, 240, 235, 0.95);
    backdrop-filter: blur(4px);
    box-shadow: 0 1px 0 rgba(26, 26, 26, 0.08);
  }

  .nav.menu-open {
    backdrop-filter: none;
  }

  .nav__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav__logo {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    text-decoration: none;
    z-index: 101;
  }

  .nav__logo-mark {
    display: flex;
    flex-direction: column;
    line-height: 0.9;
  }

  .nav__logo-top {
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--color-ink);
    text-transform: uppercase;
  }

  .nav__logo-bottom {
    font-family: var(--font-body);
    font-size: 0.5rem;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: var(--color-ink);
    text-align: center;
  }

  .nav__toggle {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 8px;
    z-index: 101;
  }

  .nav__toggle-line {
    display: block;
    width: 24px;
    height: 1.5px;
    background-color: var(--color-ink);
    transition: transform 0.3s ease, opacity 0.3s ease;
  }

  .nav__toggle[aria-expanded="true"] .nav__toggle-line:first-child {
    transform: translateY(3.75px) rotate(45deg);
  }

  .nav__toggle[aria-expanded="true"] .nav__toggle-line:last-child {
    transform: translateY(-3.75px) rotate(-45deg);
  }

  .nav__links {
    position: fixed;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-lg);
    background-color: var(--color-cream);
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .nav__links.open {
    opacity: 1;
    visibility: visible;
  }

  .nav__links a {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-ink);
    text-decoration: none;
    position: relative;
  }

  .nav__links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 1px;
    background-color: var(--color-red);
    transition: width 0.3s ease;
  }

  .nav__links a:hover::after {
    width: 100%;
  }

  @media (min-width: 768px) {
    .nav__toggle {
      display: none;
    }

    .nav__links {
      position: static;
      flex-direction: row;
      gap: var(--space-md);
      background: none;
      opacity: 1;
      visibility: visible;
    }

    .nav__links a {
      font-size: var(--text-sm);
    }
  }
</style>

<script>
  // Nav scroll effect
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 50) {
      nav?.classList.add('scrolled');
    } else {
      nav?.classList.remove('scrolled');
    }
  });

  // Mobile menu toggle
  const toggle = document.querySelector('.nav__toggle');
  const links = document.querySelector('.nav__links');
  toggle?.addEventListener('click', () => {
    const expanded = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!expanded));
    links?.classList.toggle('open');
    nav?.classList.toggle('menu-open');
    document.body.style.overflow = expanded ? '' : 'hidden';
  });

  // Close menu on link click
  links?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => {
      toggle?.setAttribute('aria-expanded', 'false');
      links?.classList.remove('open');
      nav?.classList.remove('menu-open');
      document.body.style.overflow = '';
    });
  });
</script>
